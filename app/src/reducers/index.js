//imports

//initial state
const initialState = {
  pokemon: {},
  isFetching: false,
  error: ''
}

//reducer

export const reducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state
  }

}